import React from 'react';

interface Props {
  // props here
}

const NewArrivals: React.FC<Props> = ({  }) => {
  return (
    <section className="w-screen inset-0 bg-black">
      w
    </section>
  );
};

export default NewArrivals;
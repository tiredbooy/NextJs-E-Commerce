import UserDashboard from "../_components/account/dashboard/user/UserDashboard";

interface Props {
  // props here
}

const page: React.FC<Props> = ({  }) => {
  return (
    <UserDashboard />
  );
};

export default page;